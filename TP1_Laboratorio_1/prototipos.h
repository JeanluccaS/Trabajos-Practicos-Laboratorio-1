#include <stdio.h>
#include <stdlib.h>
/** \brief funcion para sumar 2 numeros.
 *
 * \param recibe el primer numero.
 * \param recibe el segundo numero.
 * \return devuelve el resultado de la suma.
 *
 */
float sumar (float numero1, float numero2);

/** \brief funcion para restar 2 numeros
 *
 * \param recibe el primer numero
 * \param recibe el segundo numero
 * \return devuelve el resultado de la resta
 *
 */
float restar (float numero1, float numero2);
/** \brief funcion para multiplicar 2 numeros.
 *
 * \param recibe el primer numero.
 * \param recibe el segundo numero.
 * \return devuelve el resultado del producto.
 *
 */

float multiplicar (float numero1, float numero2);
/** \brief funcion para dividir 2 numeros.
 *
 * \param recibe el primer numero.
 * \param recibe el segundo numero.
 * \return devuelve el resultado de la division.
 *
 */
float dividir (float numero1, float numero2);
/** \brief funcion para pedir un numero.
 *
 * \param recibe una cadena de caracteres con lo que se va a pedir.
 * \return devuelve una peticion de un numero.
 *
 */
float pedirNumero(char mensaje[]);
/** \brief funcion para factorizar un numero.
 *
 * \param recibe el numero a factorizar.
 * \return devuelve el numero factorizado.
 *
 */
int factorial(int);
